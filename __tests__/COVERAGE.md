# Coverage Manifest

This manifest maps every behavior entry in [specs/testing.md](../specs/testing.md) to the tests that exercise it. Update it in the same change that adds or alters spec behavior or tests. The Tests column uses globally unique test names from the split suite; grep a name to locate its area-organized file.

Status values:

- **covered** — the entry's required cases have verifying assertions.
- **partial** — some required cases are proven; the Missing column lists the rest.
- **uncovered** — no test asserts the behavior.

Audit summary (2026-09-02): 150 entries — 148 covered, 1 partial, 1 deferred, 0 uncovered.

## Documents

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `doc.directive` | covered | "requires the Arc directive" | — |
| `doc.multi-arcs` | covered | "parses multiple arcs in one document"; "rejects a document-level cell declaration instead of discarding it"; "rejects other statements at document level" | — |
| `doc.arc-imports` | covered | "parses aliased named imports and set() from cell references"; "rejects default imports"; "uses direct root node declarations and rejects export syntax" | — |
| `doc.host-imports` | covered | "parses Arc into the new root/node IR"; "requires host module imports to be default imports"; "requires standalone host calls to be declared by host module import"; "rejects host import aliases that collide with Arc built-in roots and namespaces" | — |
| `doc.comments` | covered | "ignores line and block comments across Arc source" | — |
| `doc.validation` | covered | "validates finalization-only expressions in public Document IR"; "validates hand-crafted enter target binding invariants"; "validates duplicate public IR bindings and node import visibility"; "validates node writeDiffMode in public Document IR"; "rejects raw public IR containing %s before cloning" | — |
| `doc.numeric-validity` | covered | "rejects a non-finite numeric literal in %s before IR is returned"; "gives parser-wide non-finite diagnostics precedence over contextual signed syntax"; "uses an unambiguous public-IR path for object key %j"; "validates hand-built numeric IR without mutating it"; "rejects legacy numeric specs and malformed observeAs in public IR"; "rejects malformed public arithmetic operators and local strata"; "validates every canonical numeric public-IR refinement"; "validates Runtime.add atomically and normalizes only its private clone" | — |
| `doc.element-ids` | covered | "editing one SEG leaves every other SEG's ids unchanged"; "editing one if-branch leaves the other branch's ids unchanged"; "stamping is idempotent"; "validate reports non-canonical ids without mutating its input"; "Runtime.add stamps its clone, registering unstamped hand-built IR with owner-qualified inherited-hook briefs"; "anonymous-copy refs round-trip element ids nested under if and invoke"; "assigns structural element ids scoped by SEG"; "artifact.construct blocks and resumes a nested host path exactly once"; "artifact.construct cloning preserves the path subtree"; "artifact.construct normalization distinguishes constructor paths" | — |

## Spec System

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `spec.type-ownership` | covered | "exposes public domain types through their intended entry points"; "artifact-array.type-ownership exports stored and primitive array domains from public barrels"; TypeScript build verifies the `arc/types`, `arc/parser`, and `arc/runtime` declarations | — |
| `spec.representation` | covered | "validates semantic invariants outside the TypeScript representation"; "artifact-array.declaration parses cell and channel specs and rejects invalid element forms"; "artifact-array.public-ir rejects declaration metadata on Artifact elements through validate and Runtime.add"; declaration-shape coverage in "accepts call-form declarations for every cell type" and "artifact.cell validates optional public initializer shapes" | — |
| `spec.concrete-admission` | covered | "admits concrete values under the full landing constraint"; "distinguishes an unset value from null during concrete admission"; "artifact-array.channels clone direct args and cross child and root returns"; "artifact-array.persistence rejects malformed restored members before replay"; "artifact-array.host-modules admit whole arguments and typed results with indexed diagnostics"; "proto.arc-entry validates every channel family before execution"; "proto.persistence revalidates restored cells and by-value channels against document specs"; "proto.persistence revalidates restored pre-snapshot $case values"; "rejects a restored arena with an invalid $case before member replay"; observation number/Enum/array validation suites | — |
| `spec.one-time-landing` | covered | "judges site guarantees directionally"; "judges exact literals, carrier facts, and intrinsic provenance"; "judges arrays recursively and empty arrays contextually"; "artifact-array.conditional landing accepts only branches compatible with Artifact elements" | — |
| `spec.conditional-reduction` | covered | "reduces reachable conditional alternatives deterministically"; "rejects a conditional landing when any branch is incompatible"; "artifact-array.conditional landing accepts only branches compatible with Artifact elements" | — |
| `spec.reusable-compatibility` | covered | "requires reusable providers to guarantee every receiver landing"; "validates imported reusable bindings in either add order"; "artifact-array.runtime-init validates imported array and map bindings in either add order"; "treats channel assignment as directional from Index to Num" | — |
| `spec.producer-provenance` | covered | "uses the declaration to distinguish Artifact and object-shaped arguments"; "environment free analysis retains dynamic host call evidence"; "runtime add uses declared results in boolean numeric string artifact and comparison rules"; "typed Artifact results use their declaration rather than payload shape"; "artifact.semantic-expression projects an Artifact-valued conditional"; "artifact.value-template projects local, channel, and conditional values to ordinary strings"; "selects Artifact authority for array equality without inferring it from object shape"; "rejects whole Artifact-array interpolation and preserves indexed Artifact projection"; "artifact-array.map carries Artifact authority through span.item and span.result"; "carries map receiver and result element specs into imported bindings" | — |
| `spec.operation-coherence` | covered | "pairs arithmetic producer checks, concrete admission, and result evidence"; "lets Num.isFinite inspect every numeric carrier and owns its Bool result"; "pairs boolean and string operand checks with strict carrier admission"; "selects and enforces one unambiguous Enum comparison domain"; "uses ordinary string semantics for Enum equality"; "preserves and strictly admits the shared array element family"; "pairs Artifact path and interpolation projections with producer evidence"; "selects Artifact authority for array equality without inferring it from object shape"; "rejects whole Artifact-array interpolation and preserves indexed Artifact projection"; "artifact-array.cells support whole and indexed writes, reads, length, and structural equality"; static consumer suites | — |
| `spec.runtime-initialization` | covered | "rejects execution before initialization and seals after success"; "keeps collection retryable after a missing import"; "reports every independent registry issue in one initialization error"; "validates imported reusable bindings in either add order"; "artifact-array.runtime-init validates imported array and map bindings in either add order"; "rejects channel keys absent from an imported target signature"; "carries map receiver and result element specs into imported bindings"; "rejects an incompatible imported map span.item in either add order"; "rejects an incompatible imported map span.index in either add order"; "rejects an incompatible imported map span.result in either add order" | — |
| `spec.pin-replay` | covered | "does not pin an invalid typed left result while $case waits on the right"; "does not persist a non-finite compound pin while a later operand blocks"; "accepted typed results pin and replay without persisted spec evidence"; "restored pins rederive host result evidence from the original action path"; "artifact-array.pin-replay reuses typed host results without persisting provenance"; "rejects a restored pin whose durable carrier invariant was corrupted" | — |

## Host Modules

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `host.module-spec` | covered | "hmd define normalizes operations and nested namespaces"; "hmd define captures positional parameters and optional results"; "hmd define reduces nested array and tuple markers"; "hmd define admits Artifact-array results and enforces remaining result restrictions"; "hmd define rejects malformed namespace trees and member names"; "hmd markers require the active operation capture"; "hmd define rejects async functions before invoking them"; "hmd define observes returned promise rejections before failing"; "hmd define rejects cross capture tokens"; "arc host utils exposes the hmd declaration surface"; "host module specs validate exact recursive public shapes" | — |
| `runtime.host-module-spec` | covered | "uses the same host-call IR throughout the action graph"; "rejects an unsigiled host call used as a standalone statement"; "standalone host calls require a dollar sigil at every authored use site"; "operations with returns may be used as expressions or standalone calls"; "standalone calls discard a declared result and block later work"; "operations without returns are rejected by value-demanding consumers only"; "runtime add rejects an undeclared imported host module atomically"; "runtime add rejects missing members and invoked namespaces atomically"; "runtime add rejects host argument arity and incompatible operands atomically"; "runtime add uses declared results in cell and return writes"; "runtime add uses declared results in boolean numeric string artifact and comparison rules"; "host calls admit concrete arguments before emitting a brief"; "this.effects stops at the first standalone host call"; "typed host call results reject only the invalid report item and reemit it"; "typed host call results preserve explicit unset independently of the result spec"; "typed Artifact results use their declaration rather than payload shape"; "trigger and action host calls enforce the same result specs"; "accepted typed results pin and replay without persisted spec evidence"; "restored pins rederive host result evidence from the original action path" | — |

## Node Structure and Config

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `node.sections` | covered | "parses Arc into the new root/node IR"; "hoists direct cell declarations across node-body statement order"; "rejects a cell declaration nested in an if action branch"; "rejects a cell declaration nested in a label action body" | — |
| `node.identity` | covered | "resolves references by structural identifier regardless of displayName" | — |
| `cfg.display-metadata` | covered | "parses this.guidance and this.description into node metadata"; "rejects non-literal displayName and description" | — |
| `cfg.guidance` | partial | "parses this.guidance and this.description into node metadata" | Guidance reaching the host — blocked on the TODO at the node IR field (the runtime does not read it yet). |
| `cfg.forgetful-entry` | covered | "uses this.forgetfulEntry for node frame cleanup"; "keeps resolved action state for the lifetime of one entry regardless of forgetfulEntry"; "preserves cells across a forgetful entry while the frame resets" | — |
| `cfg.host-params` | covered | host-params-shape suite; "rejects null host params metadata at every nesting depth"; "keeps IR-like node and instruction host params keys opaque"; "carries node host params into semantic briefs"; "merges object instruction host params over node host params"; "uses merged instruction host params for resolution host calls"; "uses merged instruction host params for resolution judgments"; "carries unset transition host params for a child that declares none" | — |
| `cfg.duplicate-assignment` | covered | "rejects duplicate this.\* assignments" for every supported node config | — |
| `cfg.hook-shapes` | covered | "requires trigger, guard, and effects callbacks to be arrow functions"; concise-body suite | — |

## Cells

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `cell.declaration-form` | covered | "accepts call-form declarations for every cell type"; former `new` forms; "rejects new when another declarator uses the call form"; "rejects a non-cell declarator mixed with a cell declaration" | — |
| `cell.enum` | covered | "compares enum values by ordinal position, not lexicographic order"; "compares enum cells by declaration order, not lexicographic order"; "uses ordinal Enum ordering and ordinary string inequality"; "uses one proven Enum domain for another matching Enum producer"; "uses ordinary string equality across Enum domains and nonmember literals"; "rejects Enum ordering against %s"; "rejects an out-of-domain typed Enum host result before ordering" | — |
| `cell.enum-invalid` | covered | "returns invalid-item when $observe() values do not match the cell type"; "applies the valid subset while an out-of-values enum item re-yields" | — |
| `cell.bool` | covered | "accepts observing config in cell declaration forms"; set/observe scenarios throughout | — |
| `cell.static-assignment-type` | covered | "statically rejects a known-incompatible $set source: %s"; "accepts compatible scalar, exact, and recursively compatible array value families"; "rejects a conditional landing when any branch is incompatible"; "rejects a returns.*.$set(...) value of an incompatible type"; "artifact.construct rejects incompatible cell and return destinations"; "runtime add uses declared results in cell and return writes" | — |
| `cell.str` | covered | "parses Str as an observable string cell"; "stores arbitrary Str values from observation and set()"; "returns invalid-item when Str observations resolve to non-strings" | — |
| `cell.num` | covered | "parses Num and RangedInt shorthand into one canonical numeric cell kind"; "compares finite Num values exactly"; "keeps observation bounds out of direct numeric writes and currentValue"; "rejects non-finite values independently at every numeric write boundary"; "normalizes negative zero when it crosses a write boundary"; "does not apply array-element observation bounds to direct writes"; "allows a fractional direct write through RangedInt array shorthand"; "does not apply results observation bounds to member output writes" | — |
| `cell.numeric-observation-spec` | covered | "rejects a numeric observeAs with %s"; "rejects invalid RangedInt bounds and arity"; "compiles numeric observation constraints into the existing flat metadata"; "fills omitted integer bounds and preserves unconstrained number metadata"; "keeps observation bounds out of direct numeric writes and currentValue"; "validates observation constraints and normalizes reported negative zero"; "rejects non-finite and unsafe integer observations without poisoning" | — |
| `cell.cursor` | covered | "rejects observing Dialog cursor cells"; "statically rejects non-cursor assignments to cursor cells"; "snapshots Dialog.cursor and counts scoped turns locally" | — |
| `cell.is-unset` | covered | "reads unset state reactively and treats falsey assigned values as set"; "distinguishes an unknown observation from an observed value"; argument and unknown-cell rejection | — |
| `cell.artifact` | covered | "parses Artifact as a non-observable local cell"; "artifact.cell uninitialized declaration starts unset and accepts a later Artifact value"; "artifact.cell validates optional public initializer shapes"; "rejects artifacts in boolean positions and non-Artifact assignments"; "artifact.cell.set replaces the initialized value and isUnset observes stored state"; "artifact.semantic-expression projects an Artifact-valued conditional"; "artifact.value-template projects local, channel, and conditional values to ordinary strings"; "artifact.equality compares logical paths without coercion"; "artifact.direct-operations reject ordering, arithmetic, regex, and property access"; "artifact.cell.unset survives reconstruction and template interpolation rejects the unset value"; "artifact-array.cells support whole and indexed writes, reads, length, and structural equality"; "artifact-array.interpolation rejects whole arrays and preserves indexed Artifact projection"; observation and activation-trigger rejection | — |
| `cell.artifact-template` | covered | "parses Artifact path templates as value expressions"; "initializes dynamic Artifact path templates from entry args before the body"; "artifact.initializer derives from earlier and bound Artifacts and survives reconstruction"; "artifact.initializer rejects an unset later Artifact interpolation"; "artifact.initializer validates the complete path after Artifact projection"; "artifact.initializer evaluates once even when its path dependency changes in the body"; "artifact.initializer never reruns on a later child entry after unset"; invalid-path and invalid-interpolation suites | — |
| `cell.observing` | covered | "accepts observing config in cell declaration forms"; duplicate declaration-key, declaration-plus-assignment, and repeated-assignment rejection; "prefers the per-call observation question over the declared observing" | — |
| `cell.bare-boolean` | covered | branch, logical, negation, ternary, nested value-expression, and hook-return lint tests; "artifact.construct preserves bare-boolean linting inside path templates"; explicit equality and `isUnset()` exclusions | — |
| `cell.boolean-position` | covered | "rejects a Str/Num/Enum/Dialog.Cursor cell used directly as a boolean" (bare, `&&`, `\|\|`, `!`, ternary-test); "accepts an explicit comparison of a non-Bool cell"; "rejects a non-boolean && operand in a value position"; "rejects a non-boolean value in a trigger if-test"; "rejects a non-boolean trigger return"; "rejects a non-boolean catchDeflection return"; "checks statically typed array elements and map spans in boolean positions"; "runtime add uses declared results in boolean numeric string artifact and comparison rules"; array cases under "rejects ordering, boolean, and negation of a whole array at parse" and "rejects a whole-array channel in a boolean position" | — |
| `cell.dead-read` | covered | lint suite; "artifact-array.dead-read reports never-assigned and read-before-set arrays without flagging initialized Artifacts"; "treats a decorated write as a root read rather than initialization"; "records a dynamic target accessor as a cell read"; "counts a $map results array as an assignment of that cell"; "errors on a $map over a receiver array that is never assigned"; "errors on a $map callback reading a cell nothing assigns"; "accepts a cell a $map callback assigns and a later statement reads"; unset equality/boolean cases; all four ordering operators with unset on either side; unset value/semantic-template, host-call argument, and set() poison cases | — (comparisons evaluate unset values without error, by design — the resume machinery depends on it) |
| `cell.array` | covered | "parses Array(elementCell) as an array cell carrying its element spec"; "rejects a bare element constructor, nesting, and extra arguments in Array()"; "artifact-array.declaration parses cell and channel specs and rejects invalid element forms"; "artifact-array.public-ir rejects declaration metadata on Artifact elements through validate and Runtime.add"; "parses direct and decorated cell targets into root-plus-accessor tuples"; "normalizes raw nested cell-target AST accessors from root outward"; "normalizes dot access as a string accessor and rejects scalar traversal"; "rejects access past an array's scalar element"; "keeps unset direct-only and keeps returns root assignment unsupported" | — |
| `cell.array-values` | covered | "stores a whole array literal via $set"; "replaces one existing array element without changing its siblings"; "uses a dynamic index for an array-element write"; "artifact-array.cells support whole and indexed writes, reads, length, and structural equality"; "artifact-array.conditional landing accepts only branches compatible with Artifact elements"; "artifact-array.channels clone direct args and cross child and root returns"; "artifact-array.host-modules admit whole arguments and typed results with indexed diagnostics"; "poisons an element write with an %s array target"; "poisons a hand-authored negative array target index"; "poisons a non-numeric dynamic array target index"; "does not apply array-element observation bounds to direct writes"; "clears a set array via $unset"; "compares arrays structurally by value, order, and length"; "rejects equality between arrays with incompatible element guarantees"; "rejects a dynamic %s array against a numeric array before equality"; "applies the unset-comparison rule to an unset array"; "interpolates an array value into a template as comma-joined text"; "rejects ordering, boolean, and negation of a whole array at parse"; "allows a fractional direct write through RangedInt array shorthand"; "rejects non-finite values independently at every numeric write boundary" | — |
| `cell.array-read` | covered | "reads an element by bracket index and reads length"; "artifact-array.cells support whole and indexed writes, reads, length, and structural equality"; "artifact-array.interpolation rejects whole arrays and preserves indexed Artifact projection"; "poisons the traversal on an out-of-range index"; "poisons the traversal when indexing or reading length of an unset array"; "rejects reading length of a non-array cell at parse" | — |
| `cell.array-index` | covered | "supports local arithmetic in array reads and decorated targets"; "requires safe integers at every index consumer"; "treats channel assignment as directional from Index to Num"; "accepts a span.index numeric value into numeric results" | — |

## Action Forms

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `act.set` | covered | "re-walks from the top after set() changes action graph branch reachability"; "set() of the same value advances without a redundant re-walk"; "replaces one existing array element without changing its siblings"; "uses a dynamic index for an array-element write" | — |
| `act.unset` | covered | "parses unset in node bodies and hook statement subsets"; "clears a cell and re-walks the action graph"; "advances without re-walking when the cell is already unset"; argument and unknown-cell rejection; "keeps unset direct-only and keeps returns root assignment unsupported" | — |
| `act.observe` | covered | resolved/unknown/preserve tests; "carries a prior set() value as the observation brief currentValue"; "briefs and resolves one array element as a scalar cell target" | — |
| `act.observe-or-ask` | covered | "re-walks from the top after $observeOrAsk() resolves normally"; "records needs-user/proceed flow against the active owned child"; "resurfaces observeOrAsk when the host reports unknown"; "pins a dynamic observation target across needs-user and JSON restart" | — |
| `act.observe-group` | covered | "parses grouped observe forms with same-name cell bindings"; "rejects %s" (rename/computed/spread/duplicate/empty/second-arg); "rejects an unknown cell in a group"; "rejects a non-observable cell in a group"; "emits one grouped brief on the shared observation channel"; "writes every resolved field together before advancing"; "skips an unknown field and settles the group"; "writes nothing and re-emits when the report omits a field"; "writes nothing and re-emits when a field value fails its cell type"; "re-emits an observeOrAsk group while a field needs the user"; "briefs an array group field with element metadata and writes its whole list"; "writes nothing and re-emits when an array group field has a bad element" | — |
| `act.observe-array` | covered | "briefs an array observation with element metadata and writes the whole list"; "rejects an array observation whose element fails the element type"; "artifact-array.observation rejects whole, indexed, grouped, and observe-or-ask targets" | — |
| `act.instruct` | covered | "rejects resolveWhen for $instruct()"; "treats $instruct() as one-shot instructions"; "resolves a one-shot instruction only after its application report"; "keeps a one-shot instruction pending across restart and rejected reports until application"; "keeps an applied one-shot resolved through a same-handback deflection"; "re-presents an unapplied one-shot after its deflection is caught"; "evaluates deflection independently of instruction application" (banked deflect evidence) | — |
| `act.instruct-loop` | covered | "requires resolveWhen for $instructLoop()"; "prefers deflectWhen over resolveWhen when both evaluate true"; "banks a settled resolveWhen while deflectWhen stays open"; "banks a settled deflectWhen while resolveWhen stays open"; "restores a banked resolveWhen consultation onto a fresh runtime"; "resolves the loop when a banked resolveWhen meets a later deflection"; "re-emits resolution observe when terminal false repeats the instruction"; "preserves pending instruction apply and postcheck phases across fresh runtimes" | — |
| `act.instruction-text` | covered | "renders cells and ternary expressions in explicit instructions"; "preserves entity and artifact references in host-facing semantic text"; "reports invalid-template-interpolation for unset semantic-text interpolation"; "artifact-array.interpolation rejects whole arrays and preserves indexed Artifact projection" | — |
| `act.instruction-literal` | covered | "rejects bare string and template expression statements" | — |
| `act.judge` | covered | "accepts judge() inside set() value expressions"; trigger/branch judge scenarios; "resolves judge() in a set() value position from the reported boolean"; pin-lifetime cases in pins.test.ts ("a judge-gated condition before a blocking observation does not livelock"; "a pure seek reuses the pinned judgment with no repeated ids"; "a compound judge condition settles one side per brief without re-asking the other") | — |
| `act.host-call` | covered | "uses the same host-call IR throughout the action graph"; "operations with returns may be used as expressions or standalone calls"; "standalone calls discard a declared result and block later work"; "operations without returns are rejected by value-demanding consumers only"; "runtime add rejects host argument arity and incompatible operands atomically"; "host calls admit concrete arguments before emitting a brief"; "typed host call results reject only the invalid report item and reemit it"; "typed host call results accept valid siblings and retain generic sanitation"; "typed Artifact results use their declaration rather than payload shape"; "accepted typed results pin and replay without persisted spec evidence"; "restored pins rederive host result evidence from the original action path"; "uses the declaration to distinguish Artifact and object-shaped arguments"; "keeps Artifact host templates semantic while materialized paths are strings"; "rejects host calls nested inside host call arguments"; "retains one pending call and executes it again on a fresh invoke"; "resolves one host-call action per member under member-qualified ids" | — |

## Control Flow

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `flow.if-else` | covered | branch routing on cells, node state, judgments, and regex tests | — |
| `flow.label-break` | covered | label suite; "rejects break statements without a label"; nested-label unwinding tests; "a break after a resumed enter unwinds through the label-body frame"; "unwinds labeled breaks in effects, trigger, and invoke bodies at runtime" | — |
| `flow.unreached-branch` | covered | "re-walks from the top after set() changes action graph branch reachability"; "preserves resolved branch actions across later unreachable and reachable passes" | — |

## Invokes

| Id | Status | Evidence (tests) | Notes |
| --- | --- | --- | --- |
| `invoke.rerun` | covered | "re-runs a completed invoke only after a later sibling resolves with a changed read-set"; "a doubly-nested invoke constant write wins over a settled sibling set, in either order" | — |
| `invoke.continuation` | covered | "keeps an earlier branch decision while exposing an invoke write to later code"; experimental IR override: "a genuine rewalk re-reaches the invoke as a fresh invocation" | — |
| `invoke.convergence` | covered | Experimental IR override cases: "poisons a non-convergent invoke body instead of hanging"; "does not poison an idempotent convergent invoke body"; "poisons when a nested invoke write inverts its own read"; lint cases (`invoke-unconditional-set`, `invoke-self-mutating-set`, boolean-toggle pair) | — |
| `invoke.blocked-resume` | covered | "a report resumes the same blocked invocation without replaying earlier body leaves"; "a later sibling's report resumes at the sibling without re-running the invoke"; "resolves an expression-position blocker after the invoke and skips the invoke on that seek" | — |
| `invoke.dialect` | covered | "parses a block-bodied invoke into a first-class statement with body ids scoped under it"; "parses an expression-bodied invoke by wrapping the single statement"; "supports if branches, labels, and breaks scoped inside the body"; "admits enter, enterLoop, and instruct in the body"; "parses a nested invoke as an in-body statement"; "lets a nested invoke enter an enclosing node target"; "evaluates if/else branches inside the invoke body"; "admits $enter(Child) in the body, resuming through the child"; "admits enterLoop in the body"; "lints nested invokes without double-reporting the unguarded reach" | — |
| `invoke.rejections` | covered | IIFE parse-error table; invoke argument-shape table; body rejection table; "rejects invoke as a cell name"; "rejects invoke as a node name" | — |
| `invoke.scope` | covered | "records body reads in the invoke's local read-set and the enclosing node's"; "lets a body write to a parent cell show in the parent after the invocation completes"; "lets a nested invoke write a parent cell" | — |
| `invoke.deflection` | covered | "runs a fresh invocation after a deflection through the body is caught"; "a child that catches its own deflection leaves the invocation open"; "an uncaught child deflection abandons the invocation as it crosses into the body" | — |
| `invoke.refs` | covered | "surfaces the enclosing node ref as the sourceRef of an in-body instruct"; "surfaces the enclosing node ref as active/sourceRef for an in-body observe"; "inherits the enclosing node deflectWhen for an in-body instruct, including nested" | — |
| `invoke.transitions` | covered | "never latches transitions for invoke runs" | — |

## Map and Span

| Id | Status | Evidence (tests) | Notes |
| --- | --- | --- | --- |
| `map.value-transform` | covered | "commits the callback output array to results in input order"; "binds span.index and builds one ordered output per member"; "accepts a concise expression-bodied callback"; "replaces the receiver in one write when results is the receiver"; "retains the last reachable span.result write in a member"; "poisons when a member sets no span.result while results is bound"; "does not apply results observation bounds to member output writes"; "artifact-array.map carries Artifact authority through span.item and span.result" | — |
| `map.for-each` | covered | "runs members for effects and writes no output array when results is omitted"; "runs each member exactly once and waits for its instruction application" | — |
| `map.empty-input` | covered | "resolves with an empty output array over an empty input" | — |
| `map.enter-callback` | covered | "binds span.item into a child arg and span.result from a child return sink"; "captures span.index by value into an Index() child argument"; "resumes a member through a briefing child and commits its return into span.result"; "artifact-array.map admits child args and child returns through Artifact span authority" | — |
| `map.continuation` | covered | "skips a completed map while seeking a later frontier"; default sequential map cases; experimental IR override cases for containing/member restart and the non-convergence bound; "re-reads an args-channel receiver on a forgetful re-entry of the mapping node" | — |
| `map.member-blocks` | covered | "suspends a briefing member and resumes it into the same member on report"; "resumes each member on its own brief across a multi-element input"; "qualifies member brief sites so two members carry distinct brief ids"; "keeps a span.result staged before a block through report resume"; "artifact-array.map restores pinned and staged Artifact values across JSON"; "runs each member exactly once and waits for its instruction application"; "preserves an inherited instruction hook while the member awaits application" | — |
| `map.deflection` | covered | "clears the arena when a member deflects and the node catch re-reaches a virgin map"; "deflects the node with no commit when a member deflection is not caught"; "discards already-terminal members when a later member deflects"; "re-reads a receiver the catch replaced when the map restarts"; "keeps earlier members' cell writes applied after the arena is abandoned"; "keeps an earlier member's resolved host call after the arena is abandoned"; "aborts the map when a deflection escapes an entered child"; "canonicalizes the deflection through the callback so escaped matches the authored target"; "keeps the member running when the entered child catches its own deflection" (drives that recovered member to completion) | — |
| `map.json-restart` | covered | "resumes a blocked member across a JSON round-trip with earlier terminals intact"; "artifact-array.map restores pinned and staged Artifact values across JSON"; "rejects a restored arena with an invalid $case before member replay" (pinned input, terminal result, staged result); "round-trips span-backed enter links while a member's child is blocked"; "preserves a span.result staged before a block across a JSON round-trip"; "preserves a staged result while an instruction awaits application across a JSON round-trip" | — |
| `map.span-types` | covered | "rejects assigning span.item to a cell of an incompatible kind"; "checks span.item against a decorated target's element type"; "rejects a span.result write whose value kind mismatches the results element"; "rejects a span.result write of a literal of the wrong kind"; "rejects binding span.item into a child arg of an incompatible type"; "accepts a span.index numeric value into numeric results"; "carries map receiver and result element specs into imported bindings"; "artifact-array.runtime-init validates imported array and map bindings in either add order"; "artifact-array.map carries Artifact authority through span.item and span.result"; "artifact-array.map admits child args and child returns through Artifact span authority"; "rejects an incompatible imported map span.item in either add order"; "rejects an incompatible imported map span.index in either add order"; "rejects an incompatible imported map span.result in either add order" | — |
| `map.rejections` | covered | "rejects a nested $map"; "rejects a cell declaration in a $map callback"; "rejects a non-newcopy enter target in the callback"; "rejects a non-newcopy enter target nested inside a callback invoke"; "rejects a write to the receiver inside the callback"; "rejects an element write to the receiver inside the callback"; "rejects an $unset of the receiver inside the callback"; "rejects an observation of the receiver inside the callback"; "rejects a callback enter that sinks a return into the receiver"; "rejects span.result in a forEach $map"; "rejects a forEach callback that binds span.result as a return sink"; "rejects span.result bound as an args source"; "rejects span.item bound as a returns sink"; "rejects a span.item read outside a $map callback"; "rejects a span.result write outside a $map callback"; "rejects a span binding in an $enter outside a $map callback" | — |

## Hooks

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `hook.trigger-eval` | covered | trigger brief/batching/branch-scoping/host-call/regex tests | — |
| `hook.trigger-state` | covered | enterCount, seeding, unknown-observation, and deps tests | — |
| `hook.trigger-match` | covered | ambiguous-match, auto-select, fan-out, and multi-round tests; settlement cases in hooks.test.ts ("defers implicit auto-selection while another candidate has a pending judgment"; "defers implicit auto-selection while another candidate has a pending observation"; "defers implicit auto-selection while another candidate has a pending host call"; "an explicit matchable preferredMatch settles despite other pending trigger work"; "returns trigger-match-not-matchable when a retained preferredMatch settles unmatched beside one match"; "returns trigger-match-not-matchable instead of ambiguity when a retained preferredMatch settles unmatched beside multiple matches"); consultation cases in pins.test.ts ("a trigger retry seeks the candidate's answered judge"; "terminal candidates keep their outcomes and a chain-sent preferred match is retained"; "does not rerun a terminal match while another trigger candidate remains open"; "a fresh startTrigger starts new consultations") | — |
| `hook.deflect-when` | covered | inheritance/override tests; deflection-derivation tests; "an inherited deflectWhen poses a distinct judgment per owning instruction"; "preserves an inherited instruction hook while the member awaits application" | — |
| `hook.catch-deflection` | covered | "a catch consultation seeks its answered judge while a later leaf blocks"; "parses catchDeflection with deflection.escaped(), labels, and set()"; outside-finalization rejection; non-bare-target/body validation; "accepts deflection.escaped() referencing the enclosing node ..."; "returns false for escaped(Self) on the node's own frontier deflection"; "matches escaped(Child) against a newcopy(Child) entry"; "allows deflection.escaped() naming a child the node entered"; immediate-child versus nested-origin matching; catch/false-catch/resumed-catch/effects-ordering tests, including unset state during catch and deflected effects | — |
| `hook.guard` | covered | "blocks on a semantic guard and records skipped state on the owned child traversal" (skip); "lets a terminal child guard deflect its run for later re-entry" (deflect); "covers a node from its guard without running its body" (cover); forgetful-entry and acknowledging-dialog tests | — |
| `hook.effects-run` | covered | sequential/best-effort/early-stop/caught-vs-uncaught tests; pending-state and pending-deflection expression parsing; outside-effects rejection suite; covered-effects pending-deflection poison; covered and deflected pending-state exposure without early terminal-state commit | — |
| `hook.effects-host` | covered | "uses the same host-call IR throughout the action graph"; "standalone host calls require a dollar sigil at every authored use site"; "all host-call briefs carry unsigiled operation names"; "this.effects stops at the first standalone host call"; "re-surfaces an unreported host call under one id until it is reported"; "a resolved this.effects host call advances"; "accepts poison move for an unconfirmed host-call frontier"; "waits for host-call confirmation before entering the next node"; "retains one pending call and executes it again on a fresh invoke"; "resolves one host-call action per member under member-qualified ids"; "round-trips a captured host call with the same id and inputs"; restored host-call tamper cases | — |

## Composition

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `compose.children` | covered | lexical-read and lexical-owner tests | — |
| `compose.imports` | covered | "resolves imported arcs by source first, then root identifier"; "an imported arc's completion bubbles back to the caller enter"; "an imported arc deflection propagates to the caller catchDeflection"; "reads an imported arc's state and keeps its scope isolated from the caller" | — |
| `compose.node-state` | covered | "reflects a forgetful canonical entry's new terminal state through Child.state"; "covering a child re-walks a caller branch gated on its node state"; "reads an imported arc's state and keeps its scope isolated from the caller"; "rejects unknown node references in state expressions" | — |
| `compose.enter` | covered | returns-commit and covered-target tests; "does not force a forgetful entry of a covered imported arc on plain enter"; "rejects ... that re-enters the enclosing node" (bare/enterLoop/forgetful/newcopy); "rejects self-entry uniformly at a root arc" | — |
| `compose.enter-loop` | covered | "requires resolveWhen for $enterLoop()"; transactional-returns tests; "revalidates a restored enterLoop return transaction against the target signature"; "iterates enterLoop over a forgetful target with preserved child cells" | — |
| `compose.newcopy` | covered | anonymous-copy and blank-entry tests | — |
| `compose.forgetful` | covered | "forces a forgetful entry of a covered canonical child with preserved cells"; "forces a forgetful entry of a skipped canonical child and re-evaluates its guard"; "forces a forgetful entry of a covered imported arc with preserved cells"; "iterates enterLoop over a forgetful target with preserved child cells" | — |
| `compose.signature` | covered | "parses typed args/returns parameters into a node signature"; "artifact.channel.schema admits one-level Artifact arrays and keeps constructors zero-argument"; "artifact-array.declaration parses cell and channel specs and rejects invalid element forms"; "rejects the old destructured parameter form"; "rejects Index() as a cell declaration"; "rejects reserved names as channel keys"; "treats channel assignment as directional from Index to Num" | — |
| `compose.args` | covered | args-read and undeclared-poison tests; "parses $enter() renamed channel bindings decoupling caller cell names"; "reads renamed and aliased args bindings from the caller cell"; "rejects spread and computed keys in $enter() channel objects"; "rejects reading an args channel into a cell of an incompatible type"; "accepts reading an args channel into a compatible cell"; "artifact-array.channels clone direct args and cross child and root returns"; "artifact-array.runtime-init validates imported array and map bindings in either add order" | — |
| `compose.returns` | covered | commit-rule tests; "commits a renamed returns binding into the bound caller cell"; "rejects binding one caller cell to two returns keys"; "surfaces duplicate returns caller cell bindings as a structured validation issue"; "leaves caller cells unchanged for wired return keys the child never set"; effects-only rejections; "rejects a returns._.$set(...) value of an incompatible type"; "accepts a returns._.$set(...) value of a compatible type"; "artifact.channel.child forwards an Artifact through child args, shared settlement, and root returns"; "artifact.construct rejects incompatible cell and return destinations"; "artifact-array.channels clone direct args and cross child and root returns"; "artifact-array.runtime-init validates imported array and map bindings in either add order" | — |
| `compose.typed-binding` | covered | "rejects an incompatible binding"; "rejects an undeclared channel key"; "carries a Dialog.Cursor through a typed returns channel"; "artifact.channel.binding requires Artifact providers and sinks"; "validates imported reusable bindings in either add order"; "artifact-array.runtime-init validates imported array and map bindings in either add order"; "rejects channel keys absent from an imported target signature"; "carries map receiver and result element specs into imported bindings"; "rejects a length read on a non-array channel"; "rejects a whole-array channel in a boolean position"; "resolves a binding cell innermost-first when a child shadows an ancestor"; "resolves an imported binding cell innermost-first under shadowing" | — |
| `compose.unbound-channel` | covered | "reads a declared-unbound args channel as unset"; "stages a declared-unbound return and commits nothing at resolution"; "forwards a caller args projection into a child args channel"; "artifact.channel.unbound allows isUnset and rejects unset template interpolations" | — |

## Expressions and Dialog

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `expr.value-positions` | covered | "differential read-coverage: the static plan covers reads nested under every expression kind"; "parses local value expressions across expression-bearing positions" | — |
| `expr.artifact-construction` | covered | "artifact.construct lowers identical source syntax to contextual IR"; "artifact.construct rejects invalid value-site argument forms: %s"; "artifact.construct rejects a statically non-string path expression: %s"; "artifact.construct rejects invalid literal path: %s"; "artifact.construct snapshots a Str expression at the action position"; "artifact.construct poisons before mutation for unset and invalid dynamic paths"; "artifact.construct blocks and resumes a nested host path exactly once"; "artifact.construct rejects incompatible cell and return destinations"; "artifact.construct rejects invalid Artifact operations"; "artifact.construct validates public IR path shapes"; "artifact.construct reports the nested briefable expression location"; "artifact.construct preserves bare-boolean linting inside path templates"; "artifact.construct cloning preserves the path subtree"; "artifact.construct normalization distinguishes constructor paths"; "differential read-coverage: the static plan covers reads nested under every expression kind" | — |
| `expr.arithmetic` | covered | "uses distinct comparison, arithmetic, numeric-unary, and predicate IR"; "rejects unsupported numeric syntax: %s"; "statically rejects a known nonnumeric %s operand"; "statically diagnoses right and unary nonnumeric arithmetic operands"; "uses JavaScript-number arithmetic without division truncation"; "evaluates every arithmetic operator with JavaScript precedence and exact equality"; "exposes raw IEEE-754 invalid results only to arithmetic and Num.isFinite"; "keeps unset evaluation and dynamic wrong types distinct"; "rejects nonnumeric typed host results before arithmetic consumes them"; "rejects a typed %s host result before arithmetic consumes it"; "supports local arithmetic in array reads and decorated targets" | — |
| `expr.num-is-finite` | covered | "statically rejects a known nonnumeric Num.isFinite argument"; "exposes raw IEEE-754 invalid results only to arithmetic and Num.isFinite"; "allows Num.isFinite to inspect overflow and a later operation to recover"; "keeps unset evaluation and dynamic wrong types distinct"; "rejects a nonnumeric typed host result before Num.isFinite consumes it"; "rejects a typed %s host result before Num.isFinite consumes it"; "does not persist scalar overflow before a later host-call suspension" | — |
| `expr.comparison` | covered | "uses distinct comparison, arithmetic, numeric-unary, and predicate IR"; "evaluates every arithmetic operator with JavaScript precedence and exact equality"; "poisons strict writes and comparisons of non-finite arithmetic"; "rejects a non-finite operand before %s produces a result"; "does not persist a non-finite compound pin while a later operand blocks"; "does not pin an invalid typed left result while $case waits on the right"; Enum-domain tests under `cell.enum`; "compares finite Num values exactly" | — |
| `expr.regex` | covered | "evaluates regexTest expressions at runtime"; "routes on a regex test against a Str cell value"; trigger regex test; "propagates expression parser context through regex targets" | — |
| `expr.ternary` | covered | "renders cells and ternary expressions in explicit instructions"; "routes on a ternary conditional in a branch condition" | — |
| `expr.template-value` | covered | "reports invalid-template-interpolation for unset ordinary and Artifact values in value templates"; "rejects a concrete dialog cursor in a value template"; "rejects whole Artifact-array interpolation and preserves indexed Artifact projection"; "artifact-array.interpolation rejects whole arrays and preserves indexed Artifact projection"; ordinary primitive/array rendering and semantic-only parse rejection tests | — |
| `dialog.participants` | covered | "lowers Dialog participant accessors and shorthands to the same semantic references"; semantic-only value-position rejection tests | — |
| `dialog.last-user-message` | covered | "evaluates regexTest expressions at runtime" | — |
| `dialog.last-turns` | covered | "accepts Dialog snapshot globals in authored expressions"; "routes on a regex test against Dialog.lastTurns(n) text" | — |
| `dialog.replan` | covered | "replans against the latest dialog passed to progress"; trigger equivalent | — |
| `cursor.snapshot` | covered | "snapshots Dialog.cursor and counts scoped turns locally" | — |
| `cursor.diffs` | covered | "computes a signed difference between two stored cursors" | — |
| `cursor.validity` | covered | backwards/ahead/shape/non-cursor poison tests | — |
| `cursor.views` | covered | view-bound cursors suite (stamping, round-trip, same-view deltas, unstamped default); "poisons a cross-view comparison with a reason naming both views" | — |

## Protocol

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `proto.registration` | covered | "host module specs validate exact recursive public shapes"; "runtime add rejects an undeclared imported host module atomically"; "runtime add rejects missing members and invoked namespaces atomically"; "runtime init retains its separate cross document compatibility responsibility"; "throws on duplicate registration and unknown traversal arcs"; "registers multiple roots from one document by declaration identifier"; "rejects invalid public documents atomically"; "snapshots documents at registration"; "rejects raw public IR containing %s before cloning"; "rejects execution before initialization and seals after success"; "keeps collection retryable after a missing import"; "creates dormant root traversals with enterCount 0 until first entry" | — |
| `proto.trigger-scope` | covered | "scopes trigger calculation to the requested Arc refs"; "retains the requested trigger scope across progressTrigger rounds" | — |
| `proto.start` | covered | "rejects starting the action stage without an entered root"; "keeps other dormant roots dormant while the entered root runs" | — |
| `proto.artifact-value` | covered | "artifact.value.factory creates a JSON-stable value without factory provenance"; "artifact.value.guard rejects every malformed Artifact shape without throwing"; "artifact.path.classifier reports stable categories and the factory uses a coded error"; "artifact.payload clones Artifact values and structural semantic parts as ordinary structs"; "artifact.payload admits dollar-prefixed struct keys and rejects non-durable shapes"; "artifact.payload rejects malformed values before Runtime.add cloning"; "artifact.payload merge treats an Artifact shape as an ordinary struct"; "artifact.payload merge validates both operands before classifying structs"; "artifact-array.type-ownership exports stored and primitive array domains from public barrels"; "artifact-array.pin-replay reuses typed host results without persisting provenance" | — |
| `proto.arc-entry` | covered | "proto.arc-entry bypasses trigger consultation and activates only the requested Arc"; "proto.arc-entry reaches the same first frontier as trigger selection"; "proto.arc-entry validates every channel family before execution"; "proto.arc-entry forwards cloned args by value and survives traversal persistence"; "proto.arc-entry leaves omitted declared args unset"; "artifact.channel.entry preserves args, initialized locals, persistence, and covered returns"; "artifact.channel.entry rejects structural and malformed direct arguments synchronously"; "artifact-array.channels clone direct args and cross child and root returns" | — |
| `proto.action-result` | covered | "proto.action-result distinguishes frontier and terminal shapes"; "proto.action-result returns covered roots with absent, empty, and staged returns"; "proto.action-result reconstructs a blocked direct invocation and returns the same output"; "proto.action-result withholds staged returns from deflected and poisoned roots"; "proto.action-result identifies the action Arc in a multi-Arc synchronous completion" | — |
| `proto.brief-identity` | covered | payload-cloning and continuation-isolation tests; "rejects cloned brief objects as caller misuse" | — |
| `proto.moves` | covered | "rejects deflect move while an instruction brief is pending"; "keeps a one-shot instruction pending across restart and rejected reports until application"; "rejects deflect move while a standalone host call is unreported"; "captures only currently reachable postchecks and postpones deeper checks" (postcheck-phase suppression); poison-move tests | — |
| `proto.report-validation` | covered | "validates instruction application reports by id, status, and phase"; "requires the exact resolved host-call envelope and admits void resolution"; "rejects host-call value accessors without invoking them"; invalid-report/invalid-item suites for action and trigger stages | — |
| `proto.host-call-result-sanitization` | covered | "rejects null host-call results at the payload boundary"; "rejects and re-emits a recursively non-finite host-call result"; "recursively normalizes accepted host results without mutating the report"; "uses an unambiguous host-result path for object key %j"; "sanitizes trigger host-call results before they can choose a match"; "typed host call results reject only the invalid report item and reemit it"; "typed host call results accept valid siblings and retain generic sanitation"; "typed host call results preserve explicit unset independently of the result spec"; "trigger and action host calls enforce the same result specs"; "does not persist a non-finite compound pin while a later operand blocks" | — |
| `proto.observation-number-normalization` | covered | "validates observation constraints and normalizes reported negative zero"; "normalizes negative zero in array and grouped observations"; "normalizes a decorated numeric array-element observation"; "keeps grouped numeric observation validation atomic"; "rejects non-finite and unsafe integer observations without poisoning" | — |
| `proto.batching` | deferred | — | Instruction batching is dormant. The optimization cases remain under the skipped `proto.batching` suite so the batch-shaped protocol and implementation can be revisited without reconstructing them. |
| `proto.brief-fields` | covered | "evaluates deflection independently of instruction application"; sourceRef/host-params/mode/postcheck suites; semantic-text-parts tests; "preserves host-variable mentions in instruction text parts"; "carries boolean observation meta"; "carries a prior set() value as the observation brief currentValue" (with enum values meta); RangedInt meta test | — |
| `proto.poison` | covered | "preserves a runtime cause code and defaults uncategorized errors"; "accepts poison move as a terminal host contract failure"; "defaults a host poison without a reason to host-poisoned"; cause-specific runtime poison tests; "contains a poisoned trigger candidate while other arcs keep probing" | — |
| `proto.idempotence` | covered | "resumes idempotently: the same brief and report twice yield the same brief"; "re-surfaces an unreported standalone host call under one id until it is reported" | — |
| `proto.persistence` | covered | "resumes an ordinary blocked frontier from JSON state on a fresh runtime"; "proto.persistence revalidates restored cells and by-value channels against document specs"; "proto.persistence revalidates restored pre-snapshot $case values"; "artifact-array.map restores pinned and staged Artifact values across JSON"; "artifact-array.persistence rejects malformed restored members before replay"; "artifact-array.pin-replay reuses typed host results without persisting provenance"; "rejects a restored arena with an invalid $case before member replay"; "revalidates a restored enterLoop return transaction against the target signature"; "rejects a restored pin whose durable carrier invariant was corrupted"; covered/deflected "fresh runtime resumes effects" JSON-roundtrip cases; "keeps a one-shot instruction pending across restart and rejected reports until application"; "preserves pending instruction apply and postcheck phases across fresh runtimes"; "restores a banked resolveWhen consultation onto a fresh runtime"; deps tests; transition/cursor JSON round-trips | — |
| `trans.shapes` | covered | entry/exit transition tests; "carries unset transition host params for a child that declares none" | — |
| `trans.coalescing` | covered | guard-less chain and deflection coalescing tests | — |
| `trans.exclusivity` | covered | exclusivity, effects-after, and acknowledging-dialog tests | — |
| `trans.non-latching` | covered | "never latches transitions for invoke runs"; "records no pending transitions during trigger probing"; "latches no transition for a set-driven re-walk inside one node body" | — |
| `trans.persistence` | covered | "re-yields the same transition after a JSON round-trip on a fresh runtime"; "re-carries the transition when a report is rejected" | — |

## Execution Engine

| Id | Status | Tests | Missing |
| --- | --- | --- | --- |
| `seg.write-diff` | covered | "retains earlier pins and accrues later expressions after a direct write"; "keeps an earlier branch decision while exposing an invoke write to later code"; "resumes after a blocking enter whose returns change the caller"; "still catches its own instruction deflection and rewalks itself"; "sets every parsed node to advance" | — |
| `seg.experimental-rewalk` | covered | Test-only Node IR overrides retain direct-write, wide-action, scoped-pin-release, and non-convergence coverage; authored parsing always selects `advance` | — |
| `seg.resume` | covered | resumePath unit cases; owner-first positive/negative pairs; nested and alternate-path positional-resume cases | — |
| `seg.pins` | covered | pins.test.ts: default write continuation retains earlier pins and accrues later entries; pure seek; compound judge and judge-or-host-call splits; "a judge inside a resolved $set value is skipped with its statement"; "pins a dynamic observation target across needs-user and JSON restart"; "artifact.construct blocks and resumes a nested host path exactly once"; "does not pin an invalid typed left result while $case waits on the right"; "accepted typed results pin and replay without persisted spec evidence"; "restored pins rederive host result evidence from the original action path"; "rejects a restored pin whose durable carrier invariant was corrupted"; JSON round-trip persistence; experimental scoped restart coverage | — |
| `runtime.numeric-emission` | covered | "rejects non-finite value and semantic template interpolation before text"; "does not persist a non-finite compound pin while a later operand blocks"; "does not persist scalar overflow before a later host-call suspension"; "rejects non-finite outgoing host-call arguments without emission"; "normalizes negative zero in outgoing host-call arguments"; "rejects a nested non-finite host-call payload before emission"; "validates Runtime.add atomically and normalizes only its private clone" | — |
| `seg.isolation` | covered | "a blocked child's caller mutation surfaces only when the enter resolves"; "a pending instruction isolates its hook mutation until it resolves"; owner-first negative pairs | — |
| `seg.dialog-gating` | covered | "a dialog-gated ancestor branch above a blocked child stays bound when only the dialog advances"; "a nested enter's resolution under an advanced dialog does not re-walk the caller"; clean-advance pairs; "a judgment resolving under an advanced dialog with no write advances without re-asking" | — |
| `defl.propagation` | covered | catch/propagation suite across owned and imported children | — |
| `defl.restart` | covered | trigger-restart retry and enterCount tests | — |
| `defl.effects-order` | covered | deflection effects-ordering and retained-frame/forgetful-entry tests | — |

## Spec Issues Found During Audit

Recorded here so they are resolved in the specs before coverage is extended over them:

1. **Ask form mentioned in the effects section.** The effects section of arc-scripts.md describes `observeOrAsk` behavior, but effects bodies reject it at parse. The runtime's shared leaf machinery would execute it (effects already block and resume); the exclusions are the parse rejection and the effects statement union. Decide whether to admit the form in effects or move the sentence to where the form is admitted.
2. **Guidance is parsed and dropped.** `this.guidance` parses into the IR and the runtime never reads it; no brief carries it. A TODO on the node IR field tracks surfacing it to the host.
